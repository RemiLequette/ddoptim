{
  "name": "ddoptim-commwise-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Commwise app sync workflow with Git-friendly block files.",
  "scripts": {
    "sync:pull": "node scripts/sync-pull.js",
    "sync:pull:live": "node scripts/sync-pull-live.js",
    "sync:pack": "node scripts/sync-pack.js",
    "sync:push": "node scripts/sync-push.js",
    "preview": "node scripts/preview.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "iconv-lite": "^0.7.2"
  }
}
