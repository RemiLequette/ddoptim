{
  "name": "ddoptim-commwise-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DDOPTIM automated algorithm testing framework with JSON model fixtures and selective CommWise sync.",
  "scripts": {
    "sync:pull": "node scripts/sync-pull-algorithms.js",
    "sync:pull:algorithms": "node scripts/sync-pull-algorithms.js",
    "test": "vitest run",
    "test:algorithms": "vitest run tests/algorithms"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
